#spring.application.name=ODOP

# Set the server port for your Spring Boot application
#server.port=50982

# MySQL properties - comment out or remove these as you're switching to MongoDB
#spring.datasource.url=jdbc:mysql://localhost:3306/odop_project?useSSL=false
#spring.datasource.username=root
#spring.datasource.password=sa12@@ET
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MongoDB properties
# Use environment variable MONGODB_URI if available (Render), otherwise default to local
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/odop_project}
spring.data.mongodb.database=${MONGODB_DATABASE:odop_project}

# JWT Secret
jwt.secret=${JWT_SECRET}

# Hibernate properties - comment these out as Hibernate is for JPA which is used with MySQL
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.generate-ddl=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
#spring.jpa.properties.hibernate.format_sql=true

# CORS - configure if needed for your application
#spring.mvc.cors.allowed-origins=http://localhost:55320
