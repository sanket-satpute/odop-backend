package com.odop.root.config;

/**
 * Placeholder configuration.
 *
 * EmbeddedMongoConfig was removed in favor of using a system MongoDB. Keep a small
 * placeholder class to avoid classpath issues while running the application.
 */
public class EmbeddedMongoConfig {
    // Intentionally left blank.
}
